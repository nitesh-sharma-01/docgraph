# Basic Example

See `../../examples/basic/documents/service.md` for a Markdown document
that can be converted into a graph without AI.

Run:

```bash
docgraph build --config examples/basic/docgraph.yaml
```
